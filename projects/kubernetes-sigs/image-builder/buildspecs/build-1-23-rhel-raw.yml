version: 0.2

phases:
  pre_build:
    commands:
      - git config --global credential.helper '!aws codecommit credential-helper $@'
      - git config --global credential.UseHttpPath true

  build:
    commands:
      - make release IMAGE_OS=rhel IMAGE_FORMAT=raw RELEASE_BRANCH=1-23 -C $PROJECT_PATH
