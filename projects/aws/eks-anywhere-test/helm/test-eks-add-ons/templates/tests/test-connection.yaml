apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-eks-add-ons.fullname" . }}-test-connection"
  labels:
    {{- include "test-eks-add-ons.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-eks-add-ons.fullname" . }}:80']
  restartPolicy: Never
