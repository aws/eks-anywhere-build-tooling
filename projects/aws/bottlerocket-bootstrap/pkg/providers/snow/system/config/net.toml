version = 2
{{- range .dnis}}
[{{ . }}]
{{- if (eq . "eth1") }}
primary = true
{{- end }}
dhcp4 = true
{{- end }}
[eth0.static4]
addresses = ["{{ .instanceIP }}/25"]
[[eth0.route]]
to = "{{ .metadataServiceIP }}/32"
from = "{{ .instanceIP }}"
via = "{{ .defaultGateway }}"
